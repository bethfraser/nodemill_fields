#DateTime picker field

A simple datetime picker field. HTML date and time inputs are used and the values concatenated into one response. Returns a string value of the datetime that can easily be converted into a Date object in JavaScript using Date.new(datestring). 

Icon image from https://www.iconfinder.com/paomedia.

Find me at twitter.com/edinbeth.